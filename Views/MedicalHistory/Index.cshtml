@model IEnumerable<MedicalHistory>

@{
    Layout = "_Layout";
    ViewBag.Title = "Liste des antécédents";
}

<h1 class="text-center text-xl font-bold my-4">Liste des antécédents</h1>
<table class="table table-striped table-hover">
    <thead class="text-center">
        <tr>
            <th>#</th>
            <th>Nom</th>
            <th>Modifier</th>
            <th>Supprimer</th>
        </tr>
    </thead>
    <tbody class="text-center">
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.MedicalHistoryId</td>
                <td>@item.Name</td>
                <td>
                    <a asp-action="Edit" asp-controller="MedicalHistory" asp-route-id="@item.MedicalHistoryId"><i class="bi bi-pencil"></i></a>
                </td>
                <td>
                    <a asp-action="Delete" asp-controller="MedicalHistory" asp-route-id="@item.MedicalHistoryId"><i class="bi bi-trash"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-action="Add" asp-controller="Allergy" class="btn btn-primary">Ajouter un nouvel antécédent</a>
