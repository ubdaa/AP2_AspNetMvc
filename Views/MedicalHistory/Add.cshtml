@model MedicalHistory

@{
    Layout = "_Layout";
    ViewBag.Title = "Ajout d'un antécédent";
}

<div class="container py-4">
    <div class="d-flex justify-content-center">
        <div class="card shadow rounded-lg" style="width: 100%; max-width: 1100px;">
            <div class="card-header text-center" style="width: 100%;">
                <h5 class="mb-0">Ajout d'un nouvel antécédent à la base</h5>
            </div>

            <div class="card-body">
                <form asp-action="Add" asp-controller="MedicalHistory" method="post">
                    @await Html.PartialAsync("_Form", Model)
                    
                    <div>
                        <input type="submit" value="Ajouter" class="btn btn-primary"/>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>