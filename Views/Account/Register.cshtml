@model AccountViewModel

@{
    ViewBag.Title = "S'inscrire";    
    Layout = "_Layout";
}

<div class="container py-4">
    <div class="d-flex justify-content-center">
        <div class="card shadow rounded-lg" style="width: 100%; max-width: 1100px;">
            <div class="card-header text-center" style="width: 100%;">
                <h5 class="mb-0">Inscription du docteur</h5>
            </div>

            <div class="card-body">
                <form asp-action="Register" asp-controller="Account" method="post">
                    
                    @await Html.PartialAsync("_PatientForm", Model)
                    
                    <span asp-validation-summary="ModelOnly" class="text-danger"></span>
                    
                    <div class="mt-3">
                        <input class="btn btn-primary" type="submit" value="S'inscrire"/>
                    </div>
                    <a class="link link-secondary" asp-action="Login" asp-controller="Account">Se connecter ?</a>
                </form>
            </div>
        </div>
    </div>
</div>